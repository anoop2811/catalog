apiVersion: core.oam.dev/v1beta1
kind: Application
metadata:
  name: oci-app
  namespace: default
spec:
  components:
    - name: test-helm
      type: helm
      properties:
        repoType: "helm"
        url: https://stefanprodan.github.io/podinfo
        chart: podinfo
        version: '6.1.*'
